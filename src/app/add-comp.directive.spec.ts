import { AddCompDirective } from './add-comp.directive';

describe('AddCompDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCompDirective();
    expect(directive).toBeTruthy();
  });
});
